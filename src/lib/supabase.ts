/** Alias stable — implémentation dans ./supabase/client (fetch natif, sans SDK). */
export {
  isSupabaseConfigured,
  supabaseInsert,
  supabaseServerInsert,
  supabaseServerSelect,
} from "./supabase/client";
